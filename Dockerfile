# Используем как базовый образ Python
FROM python:3.10-slim

# Установим рабочую директорию
WORKDIR /app

# Скопируем файл app.py в рабочую директорию внутри контейнера
COPY app.py .

# Установим основные зависимости (при необходимости)
# RUN pip install <пакет>

# Зададим команду по умолчанию для запуска скрипта
ENTRYPOINT ["python", "app.py"]
